Site header. The real navigation is Start / Shop / Edelsteine / Über Uns / Geschenksets / Kontakt — keep that order.

```jsx
<NavBar logoSrc="assets/logo-lumorani.svg" active="Shop" cartCount={2}
  announcement="Versandkostenfrei innerhalb Deutschlands" />
```

`transparent` (glass over hero photography) is the default; use `transparent={false}` on flat pages. Links get a gold gradient underline that grows from the left on hover.
