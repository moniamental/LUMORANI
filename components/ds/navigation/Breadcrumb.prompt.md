Micro breadcrumb, uppercase, slash separators.

```jsx
<Breadcrumb items={["Start", "Shop", "Ringe", "Ruby Signet"]} />
```

11px, `--tracking-caps-tight`, muted; the current page is `--text-secondary` and non-clickable.
