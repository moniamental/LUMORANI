Toggle for optional shop services.

```jsx
<Switch label="Geschenkverpackung" checked={gift} onChange={setGift} />
```

The only pill-radius control besides chips and circular icon buttons. `onChange` receives a boolean, not an event.
