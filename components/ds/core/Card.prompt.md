Surface container for facts, testimonials, service panels and forms.

```jsx
<Card variant="glass" interactive>
  <p>Handverlesen von Samir und seinem Vater.</p>
</Card>
```

Radius stays at `--radius-card` (2px) — luxury reads square. `glass` over photography, `solid` on flat ink, `inverse` inside marble sections.
