Status marker for product media. Uppercase, tracked, 2px radius — never a pill.

```jsx
<Badge tone="gold">Unikat</Badge>
<Badge tone="outline">Half &amp; Half</Badge>
```

One badge per product card maximum. `ruby` is reserved for scarcity ("Letztes Stück").
