Checkbox for filters, newsletter consent and checkout terms.

```jsx
<Checkbox label="Ich akzeptiere die AGBs" checked={ok} onChange={e => setOk(e.target.checked)} />
```

16px box, 2px radius, gold-gradient fill with an ink tick. Labels are sentence case, German, formal-but-warm.
