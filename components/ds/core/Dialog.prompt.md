Modal for size guides, engraving info and newsletter confirmation.

```jsx
<Dialog title="Größenberatung" onClose={close} footer={<Button size="sm">Verstanden</Button>}>
  <p>Miss den Innendurchmesser deines Lieblingsrings.</p>
</Dialog>
```

The scrim is `rgba(7,7,8,.72)` + `--blur-veil`. Keep the gold top hairline; it is the brand's modal signature.
