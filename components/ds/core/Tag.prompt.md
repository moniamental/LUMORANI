Pill filter chip — the one place the brand uses a full pill radius.

```jsx
<Tag selected onClick={pick}>Rubin</Tag>
<Tag onClick={pick}>Smaragd</Tag>
```

Selected = gold hairline + 14% gold wash + `--gold-100` label. Use for stone/cut/collection filters, not for status (that is `Badge`).
