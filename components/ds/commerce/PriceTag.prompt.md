Price in Jost with a German decimal comma and trailing €.

```jsx
<PriceTag value={1290} compareAt={1490} size="lg" />
```

Never set prices in the display serif and never colour them gold — gold is for actions and eyebrows.
