Product-page variant picker for Lumorani's three stone finishes.

```jsx
<CutSelector value={cut} onChange={setCut} />
```

Defaults to the real range (Ungeschliffen / Geschliffen / Half & Half) with their German hints. Selected tile gets a gold hairline plus a 10% gold wash — no filled buttons.
